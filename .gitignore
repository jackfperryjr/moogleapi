## .NET
bin/
obj/
*.user
*.suo
.vs/
*.swp

## Environment
.env
.env.*
env.example
env.*
appsettings.Local.json
appsettings.*.Local.json
appsettings.Production.json

## Cloudflare
# Wrangler's local cache. It records which account the last deploy ran against, which is
# machine-specific and belongs to whoever ran it, not to the project.
.wrangler/

## OS
.DS_Store
Thumbs.db

## Local image review (not committed)
image-pilot/

## Design references dropped in the root to look at. Screenshots of the games are somebody
## else's copyright and this repo is public — keep them local.
sample-theme.png

## Artwork staged for hand-upload through the dashboard — the source group picture and the single
## figures split out of it. Files on their way into the bucket, not part of the project.
queens-guard/
adis-mladen-ratimir-tugeim.webp

## Art-style audit working set — scoring scripts, score files and the review page. Local to
## whoever is running the re-generation campaign, and rebuildable from the API and the bucket.
art-audit/

## Agent settings and transcripts — local to whoever is working, not part of the project
.claude/
