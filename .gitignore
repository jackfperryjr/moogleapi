## .NET
bin/
obj/
*.user
*.suo
.vs/
*.swp

## Environment
.env
.env.*
# Despite the name, env.example holds real credentials here rather than placeholders —
# the ".env.*" rule above does not match it, so it needs its own line.
env.example
env.*
appsettings.Local.json
appsettings.*.Local.json
appsettings.Production.json

## OS
.DS_Store
Thumbs.db

## Local image review (not committed)
image-pilot/
